{{- if .Values.securedClusterNamespace.include }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.securedCluster.namespace }}
  labels:
    openshift.io/cluster-monitoring: "true"
{{- end }}
