{{- if .Values.projectConfig.include }}
apiVersion: config.openshift.io/v1
kind: Project
metadata:
  name: cluster
spec:
  projectRequestTemplate:
    name: {{ .Values.template.name }}
{{- with .Values.projectConfig.projectRequestMessage }}
  projectRequestMessage: {{ . | quote }}
{{- end }}
{{- end }}
