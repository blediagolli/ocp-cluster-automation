apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml

labels:
  - includeSelectors: false
    pairs:
      operator: openshift-logging
