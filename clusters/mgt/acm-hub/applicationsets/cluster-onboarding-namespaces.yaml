apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: onboarding-namespaces
  namespace: openshift-gitops
spec:
  preserveResourcesOnDeletion: true
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - git:
        repoURL: git@github.com:blediagolli/gitops-for-organizations.git
        revision: main
        files:
          - path: "clusters/**/teams/*.yaml"
  template:
    metadata:
      name: 'onboarding-ns-{{.cluster.environment}}-{{.cluster.name}}-{{.team.name}}'
      labels:
        environment: '{{.cluster.environment}}'
        cluster: '{{.cluster.name}}'
        type: onboarding-namespaces
    spec:
      project: platform
      source:
        repoURL: git@github.com:blediagolli/gitops-for-organizations.git
        targetRevision: main
        path: charts/onboarding/namespace-config
        helm:
          ignoreMissingValueFiles: true
          valueFiles:
            - '/env/{{.cluster.environment}}/conf.yaml'
            - '/env/{{.cluster.environment}}/namespace-sizes.yaml'
            - '/{{ trimSuffix "/teams" .path.path }}/conf.yaml'
            - '/{{ trimSuffix "/teams" .path.path }}/namespace-sizes.yaml'
            - '/{{.path.path}}/{{.path.filename}}'
      destination:
        server: '{{.cluster.address}}'
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 30s
            maxDuration: 10m
            factor: 2
