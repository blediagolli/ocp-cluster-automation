# Cluster-level t-shirt size overrides. Adjusts ResourceQuota and LimitRange
# for this specific cluster's capacity. Only sizes or fields that differ from
# the environment default need to be specified here.
# Precedence: chart defaults < env/<env>/namespace-sizes.yaml < this file.
{}
