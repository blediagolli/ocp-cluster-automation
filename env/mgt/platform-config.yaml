# Environment-level platform config shared by all mgt clusters. Overrides
# chart defaults for platform charts (monitoring, ingress, catalog, etc.).
# Cluster-level platform-config.yaml can override individual settings.
catalogSource:
  include: false

ingress:
  service:
    include: false
  controller:
    include: false

config:
  include: false

machineConfig:
  include: false
