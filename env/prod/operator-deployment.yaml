# Environment-level operator Subscriptions shared by all prod clusters.
# Each key under 'operators' becomes an OLM Subscription. Cluster-level
# operator-deployment.yaml can override or disable individual operators.
