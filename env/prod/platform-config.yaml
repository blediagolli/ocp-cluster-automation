# Environment-level platform config shared by all prod clusters. Overrides
# chart defaults for platform charts (monitoring, ingress, catalog, etc.).
# Cluster-level platform-config.yaml can override individual settings.
catalogSource:
  include: false
  rh_catalog_version: v4.22

ingress:
  service:
    include: false
  controller:
    include: false
    default:
      placement: node-role.kubernetes.io/master
      replicas: 3
      syslog: true
    domains:
      - { name: intranet, http_port: 30600, https_port: 31600, replicas: 3 }
      - { name: extranet, http_port: 30700, https_port: 31700, replicas: 3 }
    syslog:
      address: 10.0.0.3

config:
  include: false

machineConfig:
  include: false
